{
  "version": 2,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.go"
    }
  ],

  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/go",
      "config": {
        "buildCommand": "./build.sh"
      }
    }
  ]
}
